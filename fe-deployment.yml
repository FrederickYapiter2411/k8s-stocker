apiVersion: apps/v1
kind: Deployment
metadata:
  name: fe-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: fe-app
  template:
    metadata:
      labels:
        app: fe-app
    spec:
      containers:
      - name: fe-app
        image: frederickyapiter/fe-app:b776e71ee58cee38b149ee003d57814154cc85a7
        imagePullPolicy: Always
        env:
          - name: REACT_APP_BACKEND  # Nama variabel tergantung framework kamu
            value: "http://localhost:5000"
        resources:
          limits:
            cpu: "250m"
          requests:
            cpu: "100m"
        ports:
        - containerPort: 3000